{
  "name": "ai-alapok",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "node --test tesztek/*.test.cjs",
    "typecheck": "tsc --noEmit",
    "test:browser": "playwright test",
    "serve": "python3 -m http.server 4173 --bind 127.0.0.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.0",
    "typescript": "^5.9.0",
    "@types/node": "^24.0.0"
  }
}
